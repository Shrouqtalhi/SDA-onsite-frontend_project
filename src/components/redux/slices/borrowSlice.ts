import { createSlice } from '@reduxjs/toolkit'
import { Book } from '../../type/type'

// const initialState: Book[] = []

// export const borrowSlice = createSlice({
//   name: 'borrow',
//   initialState: initialState,
//   reducers: {
//     addBorrowsBook: (state, action) => {}
//   }
// })
